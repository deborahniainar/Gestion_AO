version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      DATABASE_URL: sqlite:///./dev.db
      APP_NAME: Gestion AO
      JWT_SECRET: change-me
      OPENAI_API_KEY: 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
      OPENAI_MODEL: gpt-4o-mini
      OPENAI_BASE_URL: ""
    command: uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

frontend:
  build:
    context: ./frontend
    dockerfile: Dockerfile
  ports:
    - "3000:3000"
  volumes:
    - ./frontend:/app
    - node_modules:/app/node_modules
  environment:
    NODE_ENV: development
  depends_on:
    - backend
  user: "${UID}:${GID}"

backend:
  build:
    context: ./backend
    dockerfile: Dockerfile
  user: "${UID}:${UID}"  # force l'utilisateur courant
  ports:
    - "8000:8000"
  volumes:
    - ./backend:/app
